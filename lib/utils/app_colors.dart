import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE73550);     // Rojo corporativo
  static const Color accent = Color(0xFFB71C1C);      // Rojo más fuerte
  static const Color background = Color(0xFFF5F5F5);  // Fondo claro
  static const Color textTitle = Color(0xFFD32F2F);   // Títulos
  static const Color textBody = Color(0xFF757575);    // Gris para subtítulos
  static const Color buttonText = Colors.white;       // Letras de botón
  static const Color card = Colors.white;             // Fondo de Cards

  // Puedes agregar otros colores según tus necesidades
}
